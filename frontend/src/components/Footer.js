import React from "react";
import '../styles/Footer.css';

const Footer = () => {
    return (
        <footer className="footer">
            {/* The footer content has been removed, leaving just the gray rectangle figured we didnt need all the junk it had*/}
        </footer>
    );
};

export default Footer;
