# Rename this file to .env

DB_HOST=localhost
DB_USER=root
DB_PASS=*Database password*
DB_NAME=*Database name*

# This can be set to any string but it is used by Express to sign cookies sent to the browser.
# Then, when Express receives the cookies back from the browser, it can validate them appropriately
# Best to set to some long, random string
SESSION_SECRET="2sqB9lVnQKKEFlSdafXzG4ai3"

# Fake admin credentials for developer testing
# Once deployed, these will no longer be needed
DEFAULT_ADMIN_USERNAME="admin"
DEFAULT_ADMIN_PASSWORD="admin123"
DEFAULT_ADMIN_EMAIL="admin@example.com"